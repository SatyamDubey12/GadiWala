{
  "name": "gadiwala-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start:json-server": "json-server --watch data.json --port 3000"
  },
  "devDependencies": {
    "json-server": "^0.17.3"
  }
}
