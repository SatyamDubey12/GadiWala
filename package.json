{
  "name": "gadiwala-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "json-server --watch data.json --port $PORT --host 0.0.0.0"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}