{
  "name": "gadiwala-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "json-server --watch data.json --port 10000 --host 0.0.0.0"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}{
  "name": "gadiwala-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "json-server --watch data.json --port 10000 --host 0.0.0.0"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}